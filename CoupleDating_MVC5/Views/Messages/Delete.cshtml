@model CoupleDating_MVC5.Models.Messages

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Messages</h4>
    <hr />
    <div class="well well-lg alert-danger">
        <h3><i> @Html.DisplayFor(model => model.Member.ScreenName)</i> <br /> <small><u>@Html.DisplayFor(model => model.subject)</u><br /> "@Html.Raw(@Model.body)"</small></h3>
    </div>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>