@model IEnumerable<CoupleDating_MVC5.Models.PlaceTypes>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Place.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PlaceType.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Place.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PlaceType.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PlaceTypesID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PlaceTypesID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PlaceTypesID })
        </td>
    </tr>
}
</table>